# Classroom Economy Platform - Environment Configuration
# Copy this file to .env and update with your actual values

# ============================================
# REQUIRED VARIABLES
# ============================================

# Flask secret key for session security (generate with: python3 -c "import secrets; print(secrets.token_urlsafe(64))")
SECRET_KEY=your-secret-key-here-generate-with-secrets-token-urlsafe

# Database connection string
# PostgreSQL (Production): postgresql://username:password@localhost:5432/classroom_economy
# SQLite (Development): sqlite:///classroom_economy.db
DATABASE_URL=postgresql://jules_user:jules_password@localhost:5432/classroom_economy_jules

# Flask environment: development, testing, or production
FLASK_ENV=development

# Encryption key for PII (generate with: openssl rand -base64 32)
ENCRYPTION_KEY=your-encryption-key-here-generate-with-openssl-rand-base64-32

# Pepper key for additional credential hashing (generate with: python3 -c "import secrets; print(secrets.token_hex(32))")
PEPPER_KEY=your-pepper-key-here-generate-with-secrets-token-hex


# ============================================
# OPTIONAL VARIABLES
# ============================================

# Cloudflare Turnstile CAPTCHA (optional - auto-bypassed in testing)
# Get keys from: https://dash.cloudflare.com/
TURNSTILE_SITE_KEY=1x00000000000000000000AA
TURNSTILE_SECRET_KEY=1x0000000000000000000000000000000AA

# Logging configuration
LOG_LEVEL=INFO
# LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s
# LOG_FILE=app.log

# Redis configuration (for rate limiting and caching)
REDIS_URL=redis://localhost:6379
RATELIMIT_STORAGE_URI=redis://localhost:6379

# CI/CD environment detection (automatically set by GitHub Actions)
# CI=true
# GITHUB_ACTIONS=true


# ============================================
# MAINTENANCE MODE VARIABLES
# ============================================

# Enable maintenance mode (true/1/on/yes to enable)
# MAINTENANCE_MODE=false

# Badge category: maintenance, bug, security, update, feature, unavailable, error
# MAINTENANCE_BADGE_TYPE=maintenance

# Free-form description of maintenance work
# MAINTENANCE_STATUS_DESCRIPTION=We are performing scheduled system updates

# Allow system admins to bypass maintenance (true/1/on/yes)
# MAINTENANCE_SYSADMIN_BYPASS=true

# Token for bypassing maintenance via ?maintenance_bypass=<token>
# MAINTENANCE_BYPASS_TOKEN=your-secure-bypass-token

# Expected time when maintenance will end (e.g., "2025-12-05 14:00 UTC")
# MAINTENANCE_EXPECTED_END=2025-12-05 14:00 UTC

# Message shown during maintenance
# MAINTENANCE_MESSAGE=We are currently performing scheduled maintenance. Please check back soon.

# Contact email for support during maintenance
# MAINTENANCE_CONTACT=support@example.com

# UptimeRobot status page URL
# STATUS_PAGE_URL=https://stats.uptimerobot.com/your-page


# ============================================
# NOTES
# ============================================

# Security:
# - Never commit this .env file to version control
# - Generate strong random keys for SECRET_KEY, ENCRYPTION_KEY, and PEPPER_KEY
# - In production, use environment variables or a secrets manager instead of .env files

# Database:
# - PostgreSQL is required for production use
# - SQLite can be used for local development and testing
# - Ensure the database exists before running migrations

# Redis:
# - Required for rate limiting to work properly
# - Install with: sudo apt-get install redis-server (Ubuntu/Debian)
# - Or use Docker: docker run -d -p 6379:6379 redis:alpine

# Cloudflare Turnstile:
# - Optional CAPTCHA protection
# - Automatically bypassed in testing environment
# - Use test keys above for development or get real keys from Cloudflare
